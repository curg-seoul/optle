# Root env for docker-compose (copy to .env). Used only to substitute variables
# in docker-compose.yml — the per-service config lives in apps/*/.env.

# Domain Caddy serves and provisions a TLS cert for. Point an A record at the
# VM's public IP first. Required by `docker compose up`.
API_DOMAIN=api.example.com
