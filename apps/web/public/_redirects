# Netlify redirects (bundled into dist/ so manual/drag-drop deploys get them too).
# The frontend calls the backend directly via VITE_API_BASE (set at build time),
# so only the SPA fallback is needed here.
/*  /index.html  200
