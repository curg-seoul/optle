# Domain comes from the API_DOMAIN env var (set in the host environment / root
# .env; docker-compose passes it to the caddy container). Point an A record at the
# VM's public IP first — Caddy auto-provisions a Let's Encrypt cert for it.
{$API_DOMAIN} {
	reverse_proxy server:8080
}
